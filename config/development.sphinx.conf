indexer
{
  mem_limit = 64M
}

searchd
{
  address = 127.0.0.1
  port = 3312
  log = /Users/michaelschwab/projects/splurgel/log/searchd.log
  query_log = /Users/michaelschwab/projects/splurgel/log/searchd.query.log
  read_timeout = 5
  max_children = 30
  pid_file = /Users/michaelschwab/projects/splurgel/log/searchd.development.pid
  max_matches = 1000
}

source address_0_core
{
type     = mysql
sql_host = localhost
sql_user = root
sql_pass = 
sql_db   = spl_development


sql_query_pre    = SET NAMES utf8

sql_query_pre    = 
sql_query        = SELECT `addresses`.`id` * 12 + 0 AS `id` , CAST(`cities`.`name` AS CHAR) AS `city_name`, CAST(`states`.`name` AS CHAR) AS `state_name`, CAST(`addresses`.`street` AS CHAR) AS `street`, CAST(`addresses`.`name` AS CHAR) AS `name`, CAST(`addresses`.`alternate_names` AS CHAR) AS `alternate_names`, `addresses`.`id` AS `sphinx_internal_id`, 3270727197 AS `class_crc`, '3270727197' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM addresses    LEFT OUTER JOIN `cities` ON `cities`.id = `addresses`.city_id   LEFT OUTER JOIN `states` ON `states`.id = `addresses`.state_id  WHERE `addresses`.`id` >= $start   AND `addresses`.`id` <= $end    GROUP BY `addresses`.`id`  ORDER BY NULL
sql_query_range  = SELECT MIN(`id`), MAX(`id`) FROM `addresses` 
sql_query_info   = SELECT * FROM `addresses` WHERE  `id` = (($id - 0) / 12)
sql_attr_uint        = sphinx_internal_id
  sql_attr_uint        = class_crc
  sql_attr_multi       = uint subclass_crcs from field
  sql_attr_uint        = sphinx_deleted
}

index address_core
{
source = address_0_core
path = /Users/michaelschwab/projects/splurgel/db/sphinx/development/address_core
charset_type = utf-8
  morphology     = stem_en
}
index address
{
  type = distributed
  local = address_core
  charset_type = utf-8
}

source brand_0_core
{
type     = mysql
sql_host = localhost
sql_user = root
sql_pass = 
sql_db   = spl_development


sql_query_pre    = SET NAMES utf8

sql_query_pre    = 
sql_query        = SELECT `brands`.`id` * 12 + 1 AS `id` , CAST(`brands`.`name` AS CHAR) AS `name`, `brands`.`id` AS `sphinx_internal_id`, 3717453404 AS `class_crc`, '3717453404' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM brands    WHERE `brands`.`id` >= $start   AND `brands`.`id` <= $end    GROUP BY `brands`.`id`  ORDER BY NULL
sql_query_range  = SELECT MIN(`id`), MAX(`id`) FROM `brands` 
sql_query_info   = SELECT * FROM `brands` WHERE  `id` = (($id - 1) / 12)
sql_attr_uint        = sphinx_internal_id
  sql_attr_uint        = class_crc
  sql_attr_multi       = uint subclass_crcs from field
  sql_attr_uint        = sphinx_deleted
}

index brand_core
{
source = brand_0_core
path = /Users/michaelschwab/projects/splurgel/db/sphinx/development/brand_core
charset_type = utf-8
  morphology     = stem_en
}
index brand
{
  type = distributed
  local = brand_core
  charset_type = utf-8
}

source city_0_core
{
type     = mysql
sql_host = localhost
sql_user = root
sql_pass = 
sql_db   = spl_development


sql_query_pre    = SET NAMES utf8

sql_query_pre    = 
sql_query        = SELECT `cities`.`id` * 12 + 2 AS `id` , CAST(`cities`.`name` AS CHAR) AS `name`, `cities`.`id` AS `sphinx_internal_id`, 2372513034 AS `class_crc`, '2372513034' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM cities    WHERE `cities`.`id` >= $start   AND `cities`.`id` <= $end    GROUP BY `cities`.`id`  ORDER BY NULL
sql_query_range  = SELECT MIN(`id`), MAX(`id`) FROM `cities` 
sql_query_info   = SELECT * FROM `cities` WHERE  `id` = (($id - 2) / 12)
sql_attr_uint        = sphinx_internal_id
  sql_attr_uint        = class_crc
  sql_attr_multi       = uint subclass_crcs from field
  sql_attr_uint        = sphinx_deleted
}

index city_core
{
source = city_0_core
path = /Users/michaelschwab/projects/splurgel/db/sphinx/development/city_core
charset_type = utf-8
  morphology     = stem_en
}
index city
{
  type = distributed
  local = city_core
  charset_type = utf-8
}

source comment_0_core
{
type     = mysql
sql_host = localhost
sql_user = root
sql_pass = 
sql_db   = spl_development


sql_query_pre    = SET NAMES utf8

sql_query_pre    = 
sql_query        = SELECT `comments`.`id` * 12 + 3 AS `id` , CAST(`comments`.`body` AS CHAR) AS `body`, `comments`.`id` AS `sphinx_internal_id`, 1539927024 AS `class_crc`, '1539927024' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM comments    WHERE `comments`.`id` >= $start   AND `comments`.`id` <= $end    GROUP BY `comments`.`id`  ORDER BY NULL
sql_query_range  = SELECT MIN(`id`), MAX(`id`) FROM `comments` 
sql_query_info   = SELECT * FROM `comments` WHERE  `id` = (($id - 3) / 12)
sql_attr_uint        = sphinx_internal_id
  sql_attr_uint        = class_crc
  sql_attr_multi       = uint subclass_crcs from field
  sql_attr_uint        = sphinx_deleted
}

index comment_core
{
source = comment_0_core
path = /Users/michaelschwab/projects/splurgel/db/sphinx/development/comment_core
charset_type = utf-8
  morphology     = stem_en
}
index comment
{
  type = distributed
  local = comment_core
  charset_type = utf-8
}

source group_0_core
{
type     = mysql
sql_host = localhost
sql_user = root
sql_pass = 
sql_db   = spl_development


sql_query_pre    = SET NAMES utf8

sql_query_pre    = 
sql_query        = SELECT `groups`.`id` * 12 + 4 AS `id` , CAST(`groups`.`name` AS CHAR) AS `name`, `groups`.`id` AS `sphinx_internal_id`, 2885774273 AS `class_crc`, '2885774273' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM groups    WHERE `groups`.`id` >= $start   AND `groups`.`id` <= $end    GROUP BY `groups`.`id`  ORDER BY NULL
sql_query_range  = SELECT MIN(`id`), MAX(`id`) FROM `groups` 
sql_query_info   = SELECT * FROM `groups` WHERE  `id` = (($id - 4) / 12)
sql_attr_uint        = sphinx_internal_id
  sql_attr_uint        = class_crc
  sql_attr_multi       = uint subclass_crcs from field
  sql_attr_uint        = sphinx_deleted
}

index group_core
{
source = group_0_core
path = /Users/michaelschwab/projects/splurgel/db/sphinx/development/group_core
charset_type = utf-8
  morphology     = stem_en
}
index group
{
  type = distributed
  local = group_core
  charset_type = utf-8
}

source message_0_core
{
type     = mysql
sql_host = localhost
sql_user = root
sql_pass = 
sql_db   = spl_development


sql_query_pre    = SET NAMES utf8

sql_query_pre    = 
sql_query        = SELECT `messages`.`id` * 12 + 5 AS `id` , CAST(`messages`.`body` AS CHAR) AS `body`, `messages`.`id` AS `sphinx_internal_id`, 2030045667 AS `class_crc`, '2030045667' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM messages    WHERE `messages`.`id` >= $start   AND `messages`.`id` <= $end    GROUP BY `messages`.`id`  ORDER BY NULL
sql_query_range  = SELECT MIN(`id`), MAX(`id`) FROM `messages` 
sql_query_info   = SELECT * FROM `messages` WHERE  `id` = (($id - 5) / 12)
sql_attr_uint        = sphinx_internal_id
  sql_attr_uint        = class_crc
  sql_attr_multi       = uint subclass_crcs from field
  sql_attr_uint        = sphinx_deleted
}

index message_core
{
source = message_0_core
path = /Users/michaelschwab/projects/splurgel/db/sphinx/development/message_core
charset_type = utf-8
  morphology     = stem_en
}
index message
{
  type = distributed
  local = message_core
  charset_type = utf-8
}

source network_0_core
{
type     = mysql
sql_host = localhost
sql_user = root
sql_pass = 
sql_db   = spl_development


sql_query_pre    = SET NAMES utf8

sql_query_pre    = 
sql_query        = SELECT `networks`.`id` * 12 + 6 AS `id` , CAST(`networks`.`name` AS CHAR) AS `name`, `networks`.`id` AS `sphinx_internal_id`, 2939797024 AS `class_crc`, '2939797024' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM networks    WHERE `networks`.`id` >= $start   AND `networks`.`id` <= $end    GROUP BY `networks`.`id`  ORDER BY NULL
sql_query_range  = SELECT MIN(`id`), MAX(`id`) FROM `networks` 
sql_query_info   = SELECT * FROM `networks` WHERE  `id` = (($id - 6) / 12)
sql_attr_uint        = sphinx_internal_id
  sql_attr_uint        = class_crc
  sql_attr_multi       = uint subclass_crcs from field
  sql_attr_uint        = sphinx_deleted
}

index network_core
{
source = network_0_core
path = /Users/michaelschwab/projects/splurgel/db/sphinx/development/network_core
charset_type = utf-8
  morphology     = stem_en
}
index network
{
  type = distributed
  local = network_core
  charset_type = utf-8
}

source product_0_core
{
type     = mysql
sql_host = localhost
sql_user = root
sql_pass = 
sql_db   = spl_development


sql_query_pre    = SET NAMES utf8

sql_query_pre    = 
sql_query        = SELECT `products`.`id` * 12 + 7 AS `id` , CAST(`products`.`name` AS CHAR) AS `name`, `products`.`id` AS `sphinx_internal_id`, 485965105 AS `class_crc`, '485965105' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM products    WHERE `products`.`id` >= $start   AND `products`.`id` <= $end    GROUP BY `products`.`id`  ORDER BY NULL
sql_query_range  = SELECT MIN(`id`), MAX(`id`) FROM `products` 
sql_query_info   = SELECT * FROM `products` WHERE  `id` = (($id - 7) / 12)
sql_attr_uint        = sphinx_internal_id
  sql_attr_uint        = class_crc
  sql_attr_multi       = uint subclass_crcs from field
  sql_attr_uint        = sphinx_deleted
}

index product_core
{
source = product_0_core
path = /Users/michaelschwab/projects/splurgel/db/sphinx/development/product_core
charset_type = utf-8
  morphology     = stem_en
}
index product
{
  type = distributed
  local = product_core
  charset_type = utf-8
}

source store_0_core
{
type     = mysql
sql_host = localhost
sql_user = root
sql_pass = 
sql_db   = spl_development


sql_query_pre    = SET NAMES utf8

sql_query_pre    = 
sql_query        = SELECT `stores`.`id` * 12 + 8 AS `id` , CAST(`stores`.`name` AS CHAR) AS `name`, `stores`.`id` AS `sphinx_internal_id`, 1050048371 AS `class_crc`, '1050048371' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM stores    WHERE `stores`.`id` >= $start   AND `stores`.`id` <= $end    GROUP BY `stores`.`id`  ORDER BY NULL
sql_query_range  = SELECT MIN(`id`), MAX(`id`) FROM `stores` 
sql_query_info   = SELECT * FROM `stores` WHERE  `id` = (($id - 8) / 12)
sql_attr_uint        = sphinx_internal_id
  sql_attr_uint        = class_crc
  sql_attr_multi       = uint subclass_crcs from field
  sql_attr_uint        = sphinx_deleted
}

index store_core
{
source = store_0_core
path = /Users/michaelschwab/projects/splurgel/db/sphinx/development/store_core
charset_type = utf-8
  morphology     = stem_en
}
index store
{
  type = distributed
  local = store_core
  charset_type = utf-8
}

source tag_0_core
{
type     = mysql
sql_host = localhost
sql_user = root
sql_pass = 
sql_db   = spl_development


sql_query_pre    = SET NAMES utf8

sql_query_pre    = 
sql_query        = SELECT `tags`.`id` * 12 + 9 AS `id` , CAST(`tags`.`name` AS CHAR) AS `name`, `tags`.`id` AS `sphinx_internal_id`, 1002762595 AS `class_crc`, '1002762595' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM tags    WHERE `tags`.`id` >= $start   AND `tags`.`id` <= $end    GROUP BY `tags`.`id`  ORDER BY NULL
sql_query_range  = SELECT MIN(`id`), MAX(`id`) FROM `tags` 
sql_query_info   = SELECT * FROM `tags` WHERE  `id` = (($id - 9) / 12)
sql_attr_uint        = sphinx_internal_id
  sql_attr_uint        = class_crc
  sql_attr_multi       = uint subclass_crcs from field
  sql_attr_uint        = sphinx_deleted
}

index tag_core
{
source = tag_0_core
path = /Users/michaelschwab/projects/splurgel/db/sphinx/development/tag_core
charset_type = utf-8
  morphology     = stem_en
}
index tag
{
  type = distributed
  local = tag_core
  charset_type = utf-8
}

source user_0_core
{
type     = mysql
sql_host = localhost
sql_user = root
sql_pass = 
sql_db   = spl_development


sql_query_pre    = SET NAMES utf8

sql_query_pre    = 
sql_query        = SELECT `users`.`id` * 12 + 10 AS `id` , CAST(`users`.`persona` AS CHAR) AS `persona`, CAST(`users`.`login` AS CHAR) AS `login`, CAST(`users`.`name` AS CHAR) AS `name`, CAST(`users`.`hobbies` AS CHAR) AS `hobbies`, CAST(`users`.`interests` AS CHAR) AS `interests`, `users`.`id` AS `sphinx_internal_id`, 765557111 AS `class_crc`, '765557111' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM users    WHERE `users`.`id` >= $start   AND `users`.`id` <= $end    GROUP BY `users`.`id`  ORDER BY NULL
sql_query_range  = SELECT MIN(`id`), MAX(`id`) FROM `users` 
sql_query_info   = SELECT * FROM `users` WHERE  `id` = (($id - 10) / 12)
sql_attr_uint        = sphinx_internal_id
  sql_attr_uint        = class_crc
  sql_attr_multi       = uint subclass_crcs from field
  sql_attr_uint        = sphinx_deleted
}

index user_core
{
source = user_0_core
path = /Users/michaelschwab/projects/splurgel/db/sphinx/development/user_core
charset_type = utf-8
  morphology     = stem_en
}
index user
{
  type = distributed
  local = user_core
  charset_type = utf-8
}

source website_0_core
{
type     = mysql
sql_host = localhost
sql_user = root
sql_pass = 
sql_db   = spl_development


sql_query_pre    = SET NAMES utf8

sql_query_pre    = 
sql_query        = SELECT `websites`.`id` * 12 + 11 AS `id` , CAST(`websites`.`name` AS CHAR) AS `name`, CAST(`websites`.`href` AS CHAR) AS `href`, `websites`.`id` AS `sphinx_internal_id`, 2295489659 AS `class_crc`, '2295489659' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM websites    WHERE `websites`.`id` >= $start   AND `websites`.`id` <= $end    GROUP BY `websites`.`id`  ORDER BY NULL
sql_query_range  = SELECT MIN(`id`), MAX(`id`) FROM `websites` 
sql_query_info   = SELECT * FROM `websites` WHERE  `id` = (($id - 11) / 12)
sql_attr_uint        = sphinx_internal_id
  sql_attr_uint        = class_crc
  sql_attr_multi       = uint subclass_crcs from field
  sql_attr_uint        = sphinx_deleted
}

index website_core
{
source = website_0_core
path = /Users/michaelschwab/projects/splurgel/db/sphinx/development/website_core
charset_type = utf-8
  morphology     = stem_en
}
index website
{
  type = distributed
  local = website_core
  charset_type = utf-8
}
