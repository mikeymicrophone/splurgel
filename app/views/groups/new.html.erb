<h1> group</h1>

<% form_for(@group) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :administrator_id %><br />
    <%= f.select :administrator_id, User.map_name_and_id %>
  </p>
  <p>
    <%= f.label :group_type %><br />
    <%= f.select :group_type, Group.group_types %>
  </p>
  <p>
    <%= f.submit :gather %>
  </p>
<% end %>

<%= link_to 'groups', groups_path %>
<%= link_to_backup %>
