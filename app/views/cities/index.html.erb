<h1>cities</h1>

<table>
<% for city in @cities %>
  <tr>
    <td><%= link_to_name city %></td>
    <td><%= link_to_name city.state %></td>
    <td><%= link_to 'edit', edit_city_path(city) %></td>
    <td><%= attach_image_link city %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'new city', new_city_path %>
