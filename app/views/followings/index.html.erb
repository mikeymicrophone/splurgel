<h1>followings keep track of who is following who and which groups and locations</h1>

<table>
<% @followings.each do |following| %>
  <tr>
    <td><%= link_to_name following.follower %> following <%= link_to_name following.target %></td>
    <td><%= link_to 'show', following %></td>
    <td><%= link_to 'edit', edit_following_path(following) if following.follower == current_user %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'new following', new_following_path %>
