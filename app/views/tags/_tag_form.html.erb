<% form_for Tag.new do |f| %>
<% @tagging = Tagging.new(:target => obj)%>
  <%= f.text_field :name %>
  <% if obj && obj.kind_of?(ActiveRecord::Base) %>
    <%= hidden_field :tagging, :target_type %>
    <%= hidden_field :tagging, :target_id %>
  <% end %>
  <%= f.submit 'tag' %>
<% end %>
