
  <%=h @store.name %>
  administered by <%= link_to @store.administrator.name, @store.administrator %>

  <%=h @store.primary_image_id %>
  
  <% @store.locations.each do |l| %>
    <%= link_to l.street + ' ' + l.city, l %>
  <% end %>

<%= link_to 'edit', edit_store_path(@store) %> |
<%= link_to 'stores', stores_path %>
<%= link_to_backup %>