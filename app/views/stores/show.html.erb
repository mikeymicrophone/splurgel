<%=h @store.name %>  administered by <%= link_to_name, @store.administrator %><br>
<%= image_tag @store.primary_image.public_filename  if @store.primary_image %><br>

<% @store.locations.each do |l| %>
  <%= link_to l.street + ' ' + l.city, l %><br>
<% end %>

<%= link_to 'edit', edit_store_path(@store) %> |
<%= link_to 'stores', stores_path %>
<%= link_to_backup %>