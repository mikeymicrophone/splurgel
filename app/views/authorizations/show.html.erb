
  <%= link_to_name @authorization.user, 'login' %> is authorized to 
  <%= Authorization.type_hash[@authorization.authorization_type] %> at 
  <%= link_to_name @authorization.target %><br>

<%= link_to 'edit', edit_authorization_path(@authorization) if logged_in? && current_user.id == @authorization.authorizer_id %>
