<%= link_to_name @membership.user %> is a member of <%= link_to_name @membership.group %> since <%= time_ago_in_words @membership.created_at %> ago<br>
<%= follow_link @membership %>
<%= link_to 'edit', edit_membership_path(@membership) if @membership.user == current_user %>
<%= link_to 'memberships', memberships_path %>

